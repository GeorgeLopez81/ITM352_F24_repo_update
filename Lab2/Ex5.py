#Ask the user to enter an arbitary sentence. Calculate the length of the string and
#return that value.

sentence = input("Enter a sentence: ")

String_length = len(sentence)
print("You entered: " , sentence, ". It has a length of: " ,String_length)